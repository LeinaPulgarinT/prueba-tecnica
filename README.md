The project was carried out with reactjs hooks, bootstrap

1. Must have node installed
2. The repository must be cloned, in the console put the command git clone and the url of the repository

3. Bootstrap 4 and jquery and popper were installed for the project, to lower the dependencies run the command npm i, or install them again
4. To serve the application locally, run the command npm start.
